[user]
    name = balus
    email = balus@foxmail.com
[alias]
    cl = clone
    pl = pull
    st = status
    cm = commit
    co = checkout
    br = branch
    df = diff
    vdf = icdiff
    dfs = diff --staged
    rb = rebase
    mg = merge
    rso = restore
    rst = reset
    sth = stash
    cln = clean
    cfg = config
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    last = "log -1"
    undo = reset HEAD@{1}

[tig]
    tab-size = 4
[core]
    quotePath = false
